postgresql_homelab_users:
  - name: zbx_monitor
    host: "localhost"
    # cleartext password - not ideal,
    # but given that the monitor user only listens locally,  it's not a huge deal
    password: VM33ZenoVdVX55zud6eJYRSiJwE8fsie
    role: pg_monitor
